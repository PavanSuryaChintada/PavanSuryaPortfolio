# Cloudflare Pages configuration (Pages does not support [assets] - that is for Workers only)
name = "pavansuryachintada"
compatibility_date = "2025-02-01"

# Build output directory - must be dist for Vite
pages_build_output_dir = "./dist"
